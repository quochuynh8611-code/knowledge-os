# Kế Hoạch Triển Khai: Phase F7.0 — Research Dashboard (Multi-Domain)

**Ngày lập**: 2026-09-07  
**Phase**: F7.0 — Research Dashboard (Multi-Domain Knowledge Aggregator)  
**Trạng thái**: Ready for Implementation  
**Timebox**: 5–7 ngày  
**Chính sách CSDL**: Zero Database Schema Migrations (Tính toán thuần túy tại runtime)  

---

## 1. MỤC TIÊU & BẢN ĐỒ CÔNG VIỆC (6 TASKS)

```
[Phase F7.0: Research Dashboard]
├── Task 1: Aggregation Engine & Service (researchAggregationService.ts)
├── Task 2: Topic Dashboard UI Component (TopicDashboard.tsx)
├── Task 3: Research Timeline Engine & Component (researchTimelineService.ts + ResearchTimeline.tsx)
├── Task 4: Cross-Domain Full-Text Search with BM25 (researchSearchEngine.ts + ResearchSearchModal.tsx)
├── Task 5: Research Report Generator & Export (researchReportGenerator.ts + ExportReportModal.tsx)
└── Task 6: TopicDetail Integration, Wiring & Full Regression Verification
```

---

## 2. CHI TIẾT TỪNG TASK TRIỂN KHAI

### Task 1: Aggregation Engine & Service
- **File**: `src/lib/researchAggregationService.ts` (NEW)
- **Functions cần implement**:
  1. `aggregateTopicAssets(topicId: string, data: { notes: Note[]; flashcards: Flashcard[]; resources: Resource[]; reviews?: FlashcardReview[] }): TopicAssetAggregation`
  2. `calculateTopicStats(topicId: string, reviews: FlashcardReview[], notes: Note[]): TopicResearchStats`
  3. `calculateTopicRetentionTrend(reviews: FlashcardReview[], dateRange: '7d' | '30d' | '90d' | 'all'): Array<{ date: string; retentionRate: number; reviewCount: number }>`
  4. `getRecentTopicActivities(topicId: string, data: { notes: Note[]; flashcards: Flashcard[]; reviews: FlashcardReview[]; resources: Resource[] }, limit?: number): Array<TopicActivityItem>`
- **Kiểm thử**: `tests/unit/research-aggregation-service.test.ts` (100% pass các hàm tính toán, xử lý topic rỗng).

### Task 2: Topic Dashboard UI Component
- **File**: `src/components/research/TopicDashboard.tsx` (NEW)
- **Chức năng**:
  - Dải 6 thẻ KPI tổng quan: Ghi chú, Thẻ Flashcards, Tài liệu nguồn, Tỷ lệ nhớ (%), Chuỗi ngày học (Streak), Thời gian đã học (phút).
  - Biểu đồ đường SVG Retention Trend mượt mà với trục Y từ 0% đến 100%, có đường chuẩn 80% tối ưu.
  - Bộ chọn khoảng thời gian (7 ngày, 30 ngày, 90 ngày, Tất cả).
  - Danh sách 10 hoạt động gần nhất kèm biểu tượng phân loại (Ghi chú mới, Thẻ mới tạo, Phiên ôn tập, Tài liệu mới thêm).
- **Kiểm thử**: `tests/unit/topic-dashboard.test.tsx` (kiểm tra render KPI, chuyển đổi date range, empty state).

### Task 3: Research Timeline Engine & Component
- **Files**:
  - `src/lib/researchTimelineService.ts` (NEW)
  - `src/components/research/ResearchTimeline.tsx` (NEW)
- **Chức năng**:
  - `buildResearchTimeline(options)`: Gom các sự kiện từ Note, Flashcard, Review, Resource thành dòng sự kiện thống nhất.
  - Gom nhóm theo thời gian tương đối: `Hôm nay`, `Hôm qua`, `Tuần này`, `Tháng này`, `Cũ hơn`.
  - Bộ lọc: Lọc theo chủ đề (`topicId`), loại sự kiện (`NOTE_CREATED`, `FLASHCARD_CREATED`, `REVIEW_COMPLETED`, `RESOURCE_ADDED`), khoảng ngày.
  - Hỗ trợ click vào từng thẻ sự kiện để xem chi tiết hoặc điều hướng nhanh.
- **Kiểm thử**: `tests/unit/research-timeline.test.tsx` (kiểm tra phân nhóm thời gian, bộ lọc, tương tác click).

### Task 4: Cross-Domain Full-Text Search with BM25
- **Files**:
  - `src/lib/researchSearchEngine.ts` (NEW)
  - `src/components/research/ResearchSearchModal.tsx` (NEW)
- **Chức năng**:
  - Chỉ mục hóa bộ nhớ và tính điểm xếp hạng BM25 chuẩn ($k_1 = 1.2, b = 0.75$).
  - Chuẩn hóa đa ngôn ngữ (xóa dấu tiếng Việt, chuyển đổi Pāli/Sanskrit IAST) để query "satipatthana" khớp chính xác với "Satipaṭṭhāna" và "Sứ quán niệm".
  - Đánh dấu từ khóa khớp bằng thẻ `<mark>` nổi bật trong văn bản trích dẫn.
  - Bộ lọc loại thực thể (Tất cả / Ghi chú / Flashcard / Tài liệu).
- **Kiểm thử**: `tests/unit/research-search-engine.test.tsx` (kiểm tra độ chính xác BM25, IAST accent-insensitivity, highlight).

### Task 5: Research Report Generator & Export
- **Files**:
  - `src/lib/researchReportGenerator.ts` (NEW)
  - `src/components/research/ExportReportModal.tsx` (NEW)
- **Chức năng**:
  - Tạo tài liệu Markdown tổng hợp cho chủ đề: Frontmatter YAML, bảng tóm tắt chỉ số, trích yếu ghi chú, danh sách flashcard kèm độ thành thạo, tài liệu nguồn, dòng thời gian mốc chính.
  - Modal xem trước live markdown, tích chọn các phần muốn xuất.
  - Xuất file `.md` kèm UTF-8 BOM chuẩn để hiển thị tiếng Việt hoàn hảo trên mọi hệ thống.
- **Kiểm thử**: `tests/unit/research-report-generator.test.ts` (kiểm tra nội dung markdown, định dạng tiêu đề, danh sách).

### Task 6: TopicDetail Integration, Wiring & Regression
- **Files**:
  - `src/components/topics/TopicDetail.tsx` (MODIFIED)
  - `src/components/research/index.ts` (NEW)
- **Chức năng**:
  - Thêm tab "Nghiên Cứu" (Research Dashboard) vào thanh tab điều hướng của `TopicDetail.tsx`.
  - Tích hợp nút mở Search Modal (`Ctrl+Shift+F` hoặc nút tìm kiếm trên header).
  - Tích hợp nút Export Report kích hoạt `ExportReportModal`.
- **Kiểm thử & Đảm bảo chất lượng**:
  - `tests/integration/research-dashboard-integration.test.tsx`
  - Chạy full regression test suite (đảm bảo toàn bộ test case cũ và mới đều PASS 100%).
  - Chạy `npx tsc --noEmit` đạt 0 lỗi TypeScript.
  - Cập nhật `docs/PROJECT_STATUS.md` và `.github/RELEASE_NOTES_F7.0.md`.
  - Commit, tạo tag `v0.13.0` và push lên `origin/neh1`.
